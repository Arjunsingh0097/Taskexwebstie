# Gmail SMTP Configuration (Already configured in code)

# The contact form is now configured to use Gmail SMTP with:
# Email: ozwidetasklead@gmail.com
# App Password: wgnkfbyugopbzlhc
# Recipient: manager@taskex.in

# No additional configuration needed - emails will be sent automatically!
